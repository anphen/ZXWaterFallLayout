
Pod::Spec.new do |s|
  
  s.name          = "ZXWaterFallLayout"
  s.version       = "1.0.0"
  s.summary       = "一个瀑布流布局"
  s.platform      = :ios, "7.0"

  s.homepage      = "https://github.com/anphen/ZXWaterFallLayout"

  s.license       = "Apache License 2.0"

  s.author        = { "anphen" => "zxlx276@163.com" }

  s.source        = { :git => "https://github.com/anphen/ZXWaterFallLayout.git", :tag => "#{s.version}" }

  s.source_files  =  "ZXWaterFallLayout/*"

end